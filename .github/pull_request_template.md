## What changed

<!-- What this does and why. Link the issue if there is one: Fixes #123 -->

## Checklist

- [ ] `npm test` passes locally (all tests, not just the ones I touched)
- [ ] `./scripts/smoke.sh` passes — required if this touches `record`, `replay`, `check`, or `snapshot`
- [ ] New behavior has a test
- [ ] No new runtime dependencies (or the issue agreed on one)
- [ ] README updated if user-facing behavior or a flag changed

## Breaking changes

<!-- Does this change the cassette format, a command's output, or an exit code?
     If yes, say exactly how. If no, write "none". -->

none
